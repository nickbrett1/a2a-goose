# S12 — Does the published binary run on the target host?

**Status: NOT RUN — needs both hosts and a real release.** Buildkite is the only
validator (§9), and the release step only runs on `main` (it is conditional, which
is why job 17's release step read "broken" on the M0 feature branch — it was
*skipped*, not failed).

## What to prove

1. The musl static binary for each Linux triple runs on the actual DSM box, and
   the darwin binary on the Mac Studio — for the kinds of reasons static +
   `rusqlite`/bundled + `rustls` (no OpenSSL) were chosen in §2.3.
2. `scripts/check-goose.sh` passes **on the host**, with the host's goose. The
   binary refuses to start below the minimum goose (1.50.0) *before* binding a
   port — prove the refusal is the version message, not a bind error.
3. `/healthz` and `/status` answer, and `/status` reports the host's real goose
   path/version.
4. The release assets in the GitHub Release are the ones the launcher fetches and
   the ones that ran — same digest.

**Verdict: pending.** Blocked on merging M0 to `main` so the release step can
produce the first real artifact.
